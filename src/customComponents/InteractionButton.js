import {Button} from "@mui/material";

export default function InteractionButton({counter, type}) {
  const icon = ;
  return( <Button className="!w-min !h-min" variant="outlined" startIcon={counter ? counter : null} endIcon={}></Button>);
}
